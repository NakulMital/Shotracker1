gameState = 1;

function setup() {
  createCanvas(windowWidth, windowHeight);
  if (windowWidth === 414 && windowHeight === 846){
  screen1 = new Screen();
  screen1.display();
}
}

function draw() {

}
